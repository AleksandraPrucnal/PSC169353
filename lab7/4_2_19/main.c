#include <stdio.h>
#include <stdlib.h>

double foo(int n, double*tab1)
{
    int m = 0;
    for(int i=0; i<n; i++)
    {
        if(tab[i] != 0)
        {

        }
    }
}

int main()
{
    printf("Hello world!\n");
    return 0;
}
